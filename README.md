# transformer-from-scratch
GPT-style transformer from scratch in PyTorch with KV caching, RoPE, GQA, latent KV, and MoE

# Transformer from Scratch

Implementation of a GPT-style decoder-only transformer in PyTorch.

## Features
- Multi-head self-attention
- KV caching (prefill + decode)
- Rotary position embeddings (RoPE)
- Grouped-query attention (GQA)
- Latent key/value representations
- Mixture-of-Experts (MoE) layer

## Usage
Coming soon.
